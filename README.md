Central script that helps facilitate installing various packages ontop of the debian's APT-GET
Read in from the command line, creates an array and stores it into $packages.

Run in command line:
```
./install_packages.sh
```
